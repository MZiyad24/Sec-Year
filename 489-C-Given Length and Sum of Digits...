#include "iostream"
#include "bits/stdc++.h"
using namespace std;


int main()
{
    long long n,m,ans=0;
    cin>>n>>m;
    string s="",o="";
    if (n==1  ||  m==0)
    {
        if (m>=10 || n>1)
        {
            cout<<"-1 -1";
        }
        else
        {
            cout<<m<<" "<<m;
        }
    }
    else
    {
        long long check=n,b=m;
        for (long long i = 0; i <= 9,m>=0; ++i) {
            if (check==n && i==0)
            {
                continue;
            }
            if (m-i>(n-1)*9 && n>0)
            {
                continue;
            }
            else if (n!=0)
            {
                s+=to_string(i);
                m-=i;
                n-=1;
                i=-1;
            }
            else {
                if (m > 0) { cout << "-1 -1"; return 0;}
                break;
            }
        }
        s+=' ';
        n=check;m=b;
        for (long long i = 9; i >= 0,m>=0; --i) {
            if (check==n && i==0)
            {
                continue;
            }
            if (m-i<0 && n>0)
            {
                continue;
            }
            else if (n!=0)
            {
                s+=to_string(i);
                m-=i;
                n-=1;
                i=10;
            }
            else {
                if (m > 0) { cout << "-1 -1"; return 0;}
                break;
            }
        }
        cout<<s;
    }
    return 0;
}
